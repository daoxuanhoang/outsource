/** @format */

export default () => {
  return {
    header: { display: "flex", alignItems: "center" },
    uploadContent: { display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: "8px" }
  };
}
